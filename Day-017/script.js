// Day 17: Ternary operator practices and assignments
const age = 20;
const statusStr = age >= 18 ? "Adult" : "Minor";
console.log("Age status:", statusStr);

const score = 85;
const grade = score >= 90 ? "A" : score >= 80 ? "B" : "C";
console.log("Score Grade:", grade);